```dataview
table without id
link(row.file.link,row.file.aliases) as Areas,
link(filter(row.file.inlinks, (x) => x.file.name != row.file.name), row.file.inlinks.title) AS "Related Resources"
from  #project  and !"Templates"
where !archived
```
 ---

```button
name New Project
type note(function(){return this.inputEl.value}) template
action project
folder Notes
prompt true
color blue
```
^button-heac
