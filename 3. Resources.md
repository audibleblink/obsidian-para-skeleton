```dataview
table without id
link(row.file.name,row.file.aliases) AS Areas,
link(
  filter(row.file.inlinks, (x) => 
	  x.file.name != row.file.name and !contains(x.file.path, "Templates")
   ),
  file.inlinks.title
) AS Notes
from  #area and !"Templates"
where !archived
```

